create table R (
   a integer primary key,              
   b integer
);                                     
                                    

create table S (
    x integer,
    y integer,
    primary key (x,y)
);